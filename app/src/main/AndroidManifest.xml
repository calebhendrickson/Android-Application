<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.iastate.tlavan.bonappetit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddFood"
            android:label="@string/title_activity_add_food"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ViewFood" />
        <activity android:name=".FoodViewLayout" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".ShoppingList" />
        <activity android:name=".UserRegistration" />
        <activity android:name=".CreateRecipe" />
        <activity android:name=".RecipeViewLayout" />
        <activity android:name=".ViewRecipe"></activity>
        <activity android:name=".ViewNutrition"></activity>
    </application>

</manifest>
